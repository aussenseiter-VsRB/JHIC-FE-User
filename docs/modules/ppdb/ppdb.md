---
name: Ppdb Module Documentation
relation: RULES.md → modules/ppdb/
description: Documentation for the ppdb module (stub/placeholder page)
type: editable
---

# Ppdb Module

## Overview

The `ppdb` module is a **stub** page for `/ppdb` (student admissions). It currently renders a placeholder heading and has no real content yet.

## Structure

```
src/modules/ppdb/
├── ppdb.tsx          — Page component (placeholder)
├── ppdb.json         — Module metadata (empty `{}`)
└── css/ppdb.css      — Page styles (empty)
```

## Status

Placeholder only. Once content is added, move display data into `ppdb.json` (or `components/data/`) per the static-data rule, and extract any reusable UI into `components/`.
